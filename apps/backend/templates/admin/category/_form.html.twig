{{ form_start(form, {attr: {class: 'card w-75 mx-auto p-3'}}) }}
    {{ form_widget(form) }}
    
    <button type="submit" class="btn btn-primary">
        Sauvegarder
    </button>
{{ form_end(form) }}